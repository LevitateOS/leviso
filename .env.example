# LevitateOS Build Configuration
#
# All variables are optional. The dependency resolver auto-detects:
# 1. Environment variable override
# 2. Submodule at ../name (monorepo)
# 3. Downloads if neither exists
#
# If you're in the LevitateOS monorepo, most things are auto-detected.
# You usually only need this file for CI or standalone builds.

# =============================================================================
# LINUX KERNEL
# =============================================================================

# Path to local Linux kernel source tree
# Auto-detects: ../linux submodule, then downloads/linux
# LINUX_SOURCE=/home/vince/Projects/LevitateOS/linux

# Git URL for Linux kernel (only used when downloading)
# LINUX_GIT_URL=https://github.com/LevitateOS/linux.git

# Set to "1" for full git clone (default: shallow clone for speed)
# LINUX_FULL_CLONE=0

# Kernel version suffix (default: -levitate)
# KERNEL_LOCALVERSION=-levitate

# =============================================================================
# INSTALLATION TOOLS (recstrap, recfstab, recchroot)
# =============================================================================

# Path to local crate source (overrides auto-detection)
# Auto-detects: ../recstrap, ../recfstab, ../recchroot submodules
# Falls back to downloading from GitHub releases
# RECSTRAP_PATH=/home/vince/Projects/LevitateOS/recstrap
# RECFSTAB_PATH=/home/vince/Projects/LevitateOS/recfstab
# RECCHROOT_PATH=/home/vince/Projects/LevitateOS/recchroot

# Set to "1" for release builds when building from source (default: debug)
# REC_BUILD_RELEASE=1

# =============================================================================
# RECIPE PACKAGE MANAGER
# =============================================================================

# Path to pre-built recipe binary (for standalone builds)
# Monorepo users: auto-detects ../recipe submodule
# Standalone users: set this to your built recipe binary
# RECIPE_BINARY=/path/to/recipe/target/release/recipe

# =============================================================================
# ROCKY LINUX ISO (userspace source)
# =============================================================================

# Path to existing Rocky ISO file (skips download)
# ROCKY_ISO_PATH=/path/to/Rocky-10.1-x86_64-dvd1.iso

# Override Rocky Linux ISO download settings
# ROCKY_VERSION=10.1
# ROCKY_ARCH=x86_64
# ROCKY_URL=https://download.rockylinux.org/pub/rocky/10/isos/x86_64/Rocky-10.1-x86_64-dvd1.iso
# ROCKY_FILENAME=Rocky-10.1-x86_64-dvd1.iso
# ROCKY_SIZE=8.6GB
# ROCKY_SHA256=bd29df7f8a99b6fc4686f52cbe9b46cf90e07f90be2c0c5f1f18c2ecdd432d34

# =============================================================================
# INITRAMFS
# =============================================================================

# Extra kernel modules to include (comma-separated paths relative to /lib/modules/<ver>/)
# EXTRA_MODULES=kernel/drivers/nvme/host/nvme.ko.xz,kernel/drivers/ata/ahci.ko.xz

# Busybox static binary URL (for initramfs)
# BUSYBOX_URL=https://busybox.net/downloads/binaries/1.35.0-x86_64-linux-musl/busybox

# =============================================================================
# ISO CONFIGURATION
# =============================================================================

# ISO volume label (used for boot device detection: root=LABEL=X)
# IMPORTANT: Must match what init script looks for
# ISO_LABEL=LEVITATEOS

# =============================================================================
# OS BRANDING
# =============================================================================

# These values are written to /etc/os-release and other system files
# OS_NAME=LevitateOS
# OS_ID=levitateos
# OS_ID_LIKE=fedora
# OS_VERSION=1.0
# OS_VERSION_ID=1
# OS_HOME_URL=https://levitateos.org
# OS_BUG_REPORT_URL=https://github.com/levitateos/levitateos/issues
# OS_HOSTNAME=levitateos

# =============================================================================
# GITHUB (for downloading pre-built tools)
# =============================================================================

# GitHub organization for downloading recstrap/recfstab/recchroot releases
# GITHUB_ORG=LevitateOS

# =============================================================================
# EXTRA PACKAGES
# =============================================================================

# Additional RPMs to extract from Rocky ISO (comma-separated, appended to built-in list)
# EXTRA_RPMS=htop,vim-enhanced,tmux
